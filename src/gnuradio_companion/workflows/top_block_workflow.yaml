workflow:

    label: top_block
    description: "sting text description here"
    # parameters will contain the options form the options.block.yml.    
    parameters:
    actions: 
        - label: Generate
          description: "generate the flowgraph"
          method: generate  
        - label: Run
        - label: Kill
    entrypoint: gnuradio_companion.workflows.top_block:Generator
    
